<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nounapps.mareu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaReu">
        <activity android:name=".ui.meeting_list.AddMeetingActivity"
            android:label="Add Meeting"
            android:parentActivityName=".ui.meeting_list.MeetingListActivity"/>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.meeting_list.MeetingListActivity"/>
        <activity android:name=".ui.meeting_list.MeetingListActivity"
            android:label="Ma Réu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>